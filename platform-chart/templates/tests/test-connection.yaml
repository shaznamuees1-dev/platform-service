apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "platform-chart.fullname" . }}-test-connection"
  labels:
    {{- include "platform-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "platform-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
