apiVersion: v1
kind: Service
metadata:
  name: {{ include "platform-chart.fullname" . }}
  labels:
    {{- include "platform-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "platform-chart.selectorLabels" . | nindent 4 }}
